const filterByHost = require('./tools/filterByDomain');
const extractDomains = require('./tools/extractDomains');
const extractParams =  require('./tools/extractParams');
const checkHosts =  require('./tools/checkHosts');

module.exports = {
    filterByHost,
    extractDomains,
    extractParams,
    checkHosts
};